package com.knubisoft.tasks.codewars;

public class Kata8 {
    public static int[] countBy(int x, int n) {
//        return arrValues.indexOf('Sam') > -1;
//    }
// //       if (x >= 1) {
//            int[] count = new int[n];
//            for (int i = 0; i < n; i++) {
//                count[i] = x * (i + 1);
//            }
//            return count;
////        } else {
////            int[] count = {x};
////            return count;
////        }
//    }
        // smart decision from code wars with stream
        // return IntStream.rangeClosed(1, n)
        //      .map(i -> i * x)
        //      .toArray();
        return new int[0];
    }
}
//Create a function with two arguments
// that will return an array of the first (n) multiples of (x).
//
//Assume both the given number and
// the number of times to count will
// be positive numbers greater than 0.
//
//Return the results as an array
// (or list in Python, Haskell or Elixir).
